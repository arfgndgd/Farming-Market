@model AppUserVM
@{
    ViewBag.Title = "Login";
}
<h2 style="color:red">@TempData["giris"]</h2>

<main class="bg_gray">

    <div class="container margin_30">
        <div class="page_header">
            <div class="breadcrumbs">
                <ul>
                    <li><a href="@Url.Action("About","Page")">Home</a></li>
                    <li><a>Sing In</a></li>
                </ul>
            </div>

            <h1>Sing In Your Account or @Html.ActionLink("Create Account", "RegisterNow", "Register")</h1>
        </div>
        <!-- /page_header -->
        <div class="row justify-content-center">
            <div class="col-xl-6 col-lg-6 col-md-8">
                <div class="box_account">
                    <h3 class="client">Already Client</h3>
                    <div class="form_container">


                        @TempData["HesapAktifmi"]
                        @using (Html.BeginForm("Login", "Account", FormMethod.Post, new { @class = "form-signin" }))
                        {
                            if (ViewBag.AktifDegil != null)
                            {
                                <h2>@ViewBag.AktifDegil</h2>
                            }

                            @Html.TextBoxFor(x => x.AppUser.UserName, new { @class = "form-control", placeholder = "Kullanıcı ismi giriniz" })
                            <div class="divider"><span>Or</span></div>
                            @Html.TextBoxFor(x => x.AppUser.Email, new { @class = "form-control", placeholder = "Email giriniz" })
                            <br />
                            @Html.PasswordFor(x => x.AppUser.Password, new { @class = "form-control", placeholder = "Şifre Giriniz" })


                            <div class="clearfix add_bottom_15">
                                <div class="float-right"><a id="forgot" href="@Url.Action("LostPassword","Account")">Lost Password?</a></div>
                            </div>
                            <div class="text-center"><input type="submit" value="Log In" class="btn_1 full-width"></div>
                            @*<div id="forgot_pw">
                                    <div class="form-group">
                                        <input type="email" class="form-control" name="email_forgot" id="email_forgot" placeholder="Type your email">
                                    </div>
                                    <p>A new password will be sent shortly.</p>
                                    <div class="text-center"><input type="submit" value="Reset Password" class="btn_1"></div>
                                </div>*@
                        }

                    </div>

                    <!-- /form_container -->
                </div>
                <!-- /box_account -->
                <br />
                <br />
                <br />
                <br />
                <br />
                <br />
            </div>

        </div>
        <!-- /row -->
    </div>
    <!-- /container -->
</main>